name: Daily Mr Glowra Blog - US Schedule

on:
  schedule:
    # Run hourly; the workflow itself checks America/New_York so US daylight saving
    # time is handled automatically.
    - cron: '0 * * * *'
  workflow_dispatch:
    inputs:
      batch_size:
        description: 'Number of US blog posts to generate'
        required: false
        default: '4'
      force_run:
        description: 'Run immediately regardless of US posting hour'
        required: false
        type: boolean
        default: true

permissions:
  contents: write

env:
  NPM_CONFIG_REGISTRY: https://registry.npmjs.org/
  NPM_CONFIG_FETCH_RETRIES: 5
  NPM_CONFIG_FETCH_RETRY_MINTIMEOUT: 2000
  NPM_CONFIG_FETCH_RETRY_MAXTIMEOUT: 30000

jobs:
  us-schedule-check:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.check.outputs.should_run }}
    steps:
      - id: check
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            echo "Manual run: generating the requested batch immediately."
            exit 0
          fi

          US_HOUR=$(TZ=America/New_York date +%H)
          echo "Current America/New_York hour: $US_HOUR"

          case "$US_HOUR" in
            08|12|16|20)
              echo "should_run=true" >> "$GITHUB_OUTPUT"
              ;;
            *)
              echo "should_run=false" >> "$GITHUB_OUTPUT"
              ;;
          esac

  generate-and-publish:
    needs: us-schedule-check
    if: needs.us-schedule-check.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Reset npm cache and install dependencies
        run: |
          npm cache clean --force
          npm install --prefer-online --no-audit --no-fund

      - name: Generate US article for this US time slot
        run: npm run generate
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: gpt-4o-mini
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
          BLOG_BATCH_SIZE: ${{ github.event.inputs.batch_size || '1' }}

      - name: Replace generated images with strictly title-matched images
        run: node scripts/refresh-images.js
        env:
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}

      - name: Run US SEO audit
        run: node scripts/seo-audit.js

      - name: Publish article and image updates to GitHub
        run: |
          git config user.name "mrglowra-blog-bot"
          git config user.email "actions@users.noreply.github.com"
          git add posts/
          git diff --cached --quiet || git commit -m "Publish US cleaning article with relevant images"
          git push
